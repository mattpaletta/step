language: python
cache: pip

python:
  - "3.6"

install:
  - pip install mypy
  - pip install -r requirements.txt
  - pip install . -v

script:
  # - mypy -m pqdict
  - python3 -m unittest discover -v -s test
